package ai.vital.aspen.datawarehouse.db

class DBFunction {

  // low level operations on Segment RDD
  
   // Note: RDD operations are meant to be over large datasets at scale
  // However, a database style interface to individual changes is handy
  
  // Likely implemented with wrapping individual objects with an RDD and doing a 
  // join into a resulting RDD
  
  
 
  
  
}